<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Your Password</title>
    <style>
        * {
            margin: 0;
            padding: 0;
        }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background-color: #f5f5f5;
            color: #222;
        }
        .wrapper {
            padding: 20px;
            background-color: #f5f5f5;
        }
        .container {
            max-width: 512px;
            margin: 0 auto;
            background-color: #ffffff;
            padding: 60px 30px;
            text-align: center;
        }
        .logo {
            width: 60px;
            height: 60px;
            background-color: #000;
            border-radius: 12px;
            margin: 0 auto 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 32px;
            font-weight: 700;
            color: #ffffff;
        }
        .title {
            font-size: 28px;
            font-weight: 600;
            color: #000;
            margin-bottom: 40px;
            line-height: 1.3;
        }
        .content {
            text-align: left;
            margin-bottom: 40px;
        }
        .greeting {
            font-size: 15px;
            color: #c9302c;
            font-weight: 500;
            margin-bottom: 16px;
        }
        .message {
            font-size: 14px;
            color: #444;
            line-height: 1.6;
            margin-bottom: 12px;
        }
        .expiry-note {
            font-size: 13px;
            color: #666;
            line-height: 1.6;
            margin-top: 12px;
        }
        .button-wrapper {
            text-align: center;
            margin-bottom: 40px;
            margin-top: 40px;
        }
        .button {
            display: inline-block;
            padding: 14px 40px;
            background-color: #000;
            color: #ffffff;
            text-decoration: none;
            border-radius: 8px;
            font-size: 14px;
            font-weight: 600;
            letter-spacing: 0.3px;
        }
        .fallback-link {
            margin-bottom: 40px;
        }
        .fallback-title {
            font-size: 12px;
            color: #666;
            margin-bottom: 8px;
            text-align: left;
        }
        .fallback-url {
            font-size: 12px;
            color: #0f766e;
            word-break: break-all;
            text-decoration: underline;
            text-align: left;
        }
        .footer-note {
            font-size: 13px;
            color: #666;
            line-height: 1.6;
            text-align: left;
            margin-bottom: 12px;
        }
        .support-link {
            font-size: 13px;
            color: #666;
            line-height: 1.6;
            text-align: left;
        }
        .support-link a {
            color: #0f766e;
            text-decoration: underline;
        }
        .divider {
            height: 1px;
            background-color: #e5e5e5;
            margin: 40px 0;
        }
        .company-footer {
            font-size: 12px;
            color: #999;
            text-align: center;
        }
        @media (max-width: 480px) {
            .wrapper {
                padding: 12px;
            }
            .container {
                padding: 40px 20px;
            }
            .logo {
                width: 50px;
                height: 50px;
                font-size: 26px;
                margin-bottom: 30px;
            }
            .title {
                font-size: 22px;
                margin-bottom: 30px;
            }
            .content {
                margin-bottom: 30px;
            }
            .greeting {
                font-size: 14px;
                margin-bottom: 12px;
            }
            .message {
                font-size: 13px;
            }
            .expiry-note {
                font-size: 12px;
            }
            .button-wrapper {
                margin-bottom: 30px;
                margin-top: 30px;
            }
            .button {
                display: block;
                padding: 12px 24px;
                font-size: 13px;
                width: 100%;
                box-sizing: border-box;
            }
            .fallback-link {
                margin-bottom: 30px;
            }
            .fallback-title {
                font-size: 11px;
            }
            .fallback-url {
                font-size: 11px;
            }
            .footer-note {
                font-size: 12px;
                margin-bottom: 10px;
            }
            .support-link {
                font-size: 12px;
            }
            .divider {
                margin: 30px 0;
            }
            .company-footer {
                font-size: 11px;
            }
        }
    </style>
</head>
<body>
    <div class="wrapper">
        <div class="container">
            <div class="logo">HC</div>
            <h1 class="title">Reset your password</h1>
            
            <div class="content">
                <p class="greeting">Hello{{#if username}} {{username}}{{/if}},</p>
                <p class="message">We received a request to reset your password. Click the button below to create a new password.</p>
                <p class="expiry-note">For your security, this link expires in {{expires_in_minutes}} minutes.</p>
            </div>

            <div class="button-wrapper">
                <a href="{{reset_url}}" class="button">Reset Password</a>
            </div>

            <div class="fallback-link">
                <p class="fallback-title">Or copy this link:</p>
                <p class="fallback-url">{{reset_url}}</p>
            </div>

            <p class="footer-note">Didn't request this change? You can safely ignore this email. Your password won't be changed.</p>
            <p class="support-link">Need help? Contact us at <a href="mailto:{{support_email}}">{{support_email}}</a></p>

            <div class="divider"></div>

            <p class="company-footer">© {{year}} {{brand_name}}. All rights reserved.</p>
        </div>
    </div>
</body>
</html>
