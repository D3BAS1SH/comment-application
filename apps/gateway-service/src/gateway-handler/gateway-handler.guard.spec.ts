import { GatewayHandlerGuard } from './gateway-handler.guard';

describe('GatewayHandlerGuard', () => {
  it('should be defined', () => {
    expect(new GatewayHandlerGuard()).toBeDefined();
  });
});
